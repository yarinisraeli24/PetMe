{
  "dependencies": {
    "@babel/preset-env": "^7.16.11",
    "@babel/preset-react": "^7.16.7",
    "babel-jest": "^27.5.1",
    "concurrently": "^7.2.1",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "react-test-renderer": "^18.0.0"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "supertest": "^6.2.2"
  },
  "scripts": {
    "start": "npm i concurrently && concurrently \"cd server && npm run server\" \"cd client && npm start\"",
    "test": "jest --detectOpenHandles --forceExit"
  }
}
